# 은화 : 택티컬 업 동작 시나리오

> 모드: 보강 (단계 2 파싱 후, `data/parsed_skills.json` 기준. 수치는 스킬레벨 10)

## 기본 정보
- 버스트 단계: B2 / 본인 쿨다운 20s → **1사이클 작성**
- 무기: SR (최대 장탄 6, 재장전 2.0s, 계수 69.04%, 차지 1.0s, 풀차지 250%, 코어 200%,
  입력 `UP` → 발사 후 딜레이 CDN 유도 **0.38s**, 재장전 후 딜레이 **0**(유저 확인 2026-09-10))
- 코드: 작열 / 화력형 / 엘리시온 / **앱솔루트**
- 성격: **자기 강화 SR 딜러 + 팀 버퍼**. 카모플라쥬 상태 동안 일반 공격이 통째로
  방어력 무시 대미지가 되고 거기에 자기 `armor_break_dmg_pct`가 실린다. 버스트는
  1발짜리 RL 폭발탄 모드고, `엠마 : 택티컬 업`과 짝이면 양쪽에서 효과 6개가 더 열린다.

## 효과 요약 (`parsed_skills.json` 배열 순서 = 실행 순서)

| # | ID (name) | 출처 | 트리거 (timing / condition) | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 0 | **`카모플라쥬`** | 스킬1 | `burst_cast` | `stealth` (❌ 타겟팅 모델 없음) | self | 5.0s |
| 1 | **`카모플라쥬`** | 스킬1 | `full_charge_fire` / `during_full_burst` | `stealth` — **같은 이름 두 번째 경로** | self | 5.0s |
| 2 | `카모플라쥬 해제 (직접 피격)` | 스킬1 | `received_hit_count:1` (⚠️ 호출처 없음) | `remove_named_buff` (`target_effect: 카모플라쥬`) | self | — |
| 3 | `카모플라쥬 스카프` | 스킬1 | `passive` / `self_state:카모플라쥬` | `armor_break_enabled` (일반 공격 → 방어력 무시) | self | −1 |
| 4 | `카모플라쥬 스카프 2` | 스킬1 | 〃 | `armor_break_dmg_pct` **+42.24** | self | −1 |
| 5 | **`포메이션 AS`** | 스킬2 | `passive` | `crit_rate` **+8.16** | **`allies_squad`** | −1 |
| 6 | `포메이션 AS 2` | 스킬2 | `passive` | `charge_dmg_pct` **+41.81** (④에 퍼센트포인트 가산) | all_allies | −1 |
| 7 | `포메이션 AS 3` | 스킬2 | `passive` | `atk_pct` **+42.24** | self | −1 |
| 8 | `포메이션 AS 4` | 스킬2 | `passive` / `self_state:포메이션 LT` | `projectile_explosion_dmg_pct` +5.11 | all_allies | −1 |
| 9 | `포메이션 AS 5` | 스킬2 | 〃 | `armor_break_dmg_pct` **+30.97** | all_allies | −1 |
| 10 | **`내부 폭발탄`** | 스킬3 | `burst_cast` | `weapon_change` **RL** · 계수 105.6 · 차지 0.3s · 풀차지 300% · `max_ammo` 1 | self | 전탄 발사(`duration_bullets: 1`) |
| 11 | `내부 폭발탄 2` | 스킬3 | `burst_cast` | `armor_break_enabled` (모드 종속 부속 버프) | self | −1 (아래 #12가 끊는다) |
| 12 | `내부 폭발탄 3` | 스킬3 | `event:state_end:내부 폭발탄` | `remove_named_buff` (`target_effect: 내부 폭발탄 2`) | self | — |
| 13 | **`내부 폭발탄`** | 스킬3 | `hit_count:1` / `self_state:내부 폭발탄` | `received_dmg_pct` **+27.87** | target | 10.0s |

**딜 구조**: 본인 딜 ≒ SR 풀차지 × (② `atk_pct` +42.24 · ④ 차지 배율 250 + 41.81 ·
⑤ `armor_break_dmg_pct` 42.24(+페어 30.97×2) · ⑥ 적 `received_dmg` 27.87).
**카모플라쥬가 켜져 있는가**가 ⑤ 전체를 가르는 스위치다 — 꺼져 있으면 #3·#4가 함께 죽는다.

**단계 4에서 구현한 항목** (2026-09-10):

| 항목 | 상태 | 없었으면 |
|---|---|---|
| target `allies_squad` | ✅ 구현 (`_resolve_target`) | #5가 아무에게도 안 걸린다 → 엠마의 `self_state:포메이션 AS` 영구 거짓 → 엠마 쪽 효과 6개가 함께 죽고, 은화 본인의 크리 확률도 0 |
| `stealth` · `received_hit_count` | ❌ / ⚠️ 그대로 | 딜 무영향 — **`stealth`가 미구현이어도 `self_state:카모플라쥬`는 성립한다**(상태 판정은 이름으로 한다). #3·#4가 정상 동작하는 것을 실측으로 확인했다 |

## 검증 스쿼드

- **표준 (B2 / 쿨 20s)**: `["리틀 머메이드", "은화 : 택티컬 업", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - `GAMEPLAY.md` §표준 테스트 스쿼드 B2·20초 템플릿. 리틀 머메이드의 7.48 버쿨감으로
    12.55초 균일 프로필이고, B2가 혼자라 매 사이클 본인 버스트다.
- **변형 1 「앱솔루트 페어」**: `["엠마 : 택티컬 업", "은화 : 택티컬 업", "리틀 머메이드", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - #8·#9와 엠마 쪽 6종을 여는 **유일한 조건**. 같은 12.55 프로필이라 표준과 나란히 비교된다.
- **변형 2 「RL 아군 포함」**: `["엠마 : 택티컬 업", "은화 : 택티컬 업", "홍련 : 흑영", "스노우 화이트 : 헤비암즈"]`
  - #6(차지 대미지)과 #8(발사체 폭발)이 실제 RL 차지 아군에게 실리는지.

## 사이클 타임라인 (표준 스쿼드 · 1사이클)

| t (예상) | 이벤트 | 발동 효과 (#) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.0 | `passive` | #5 `crit_rate` +8.16 / #6 `charge_dmg_pct` +41.81 / #7 `atk_pct` +42.24 | **allies_squad = 자신뿐** / 아군 전체 / self | #8·#9 **무발동**(엠마 없음) |
| 0.0 | `passive`(조건 미충족) | #3·#4 등록되나 `self_state:카모플라쥬` 거짓 → 꺼짐 | | 런타임 재평가 |
| 0.0~ | 일반 공격 | SR 풀차지(차지 1.0s + 발사 후 0.38s), 6발 후 재장전 2.0s | | 방어력 무시 **아님** |
| ~2.4 | **B2 본인 `burst_cast`** | #0 카모플라쥬 5s · #10 모드 진입(RL 1발) · #11 부속 `armor_break_enabled` | self | #3·#4가 이때 켜진다 |
| ~2.5 | 풀버스트 진입 | 스쿼드 ③ +50% | | |
| ~2.7 | 모드 1발 발사(풀차지 300%) | #13 `received_dmg_pct` +27.87 (`hit_count:1` + 모드 상태) | 적 10s | 발사 → 명중 → **전탄 소진으로 모드 종료** |
| ~2.7 | 모드 종료 | `event:state_end:내부 폭발탄` → #12가 #11 제거 · SR 만탄 복귀 · 차지 재시작 | | GAMEPLAY §무기 메카닉 |
| 2.7~12.5 | 풀버스트 딜 구간 | 풀차지마다 #1이 카모플라쥬를 **5초로 갱신** → #3·#4 유지 | self | 이 구간이 ⑤의 전부 |
| ~12.5 | 풀버스트 종료 | 마지막 갱신 +5초까지만 카모플라쥬 유지 | | 이후 #3·#4 **꺼져야 한다** |
| ~15.0 | 사이클2 | 반복 | | 12.55 균일 |

> **관찰점**: 카모플라쥬 가동률 = 버스트 시전 ~ (풀버스트 마지막 풀차지 + 5초).
> **12.55초 사이클에서는 그 5초가 다음 버스트까지 닿아 사실상 상시**가 된다(실측: OFF 구간이
> `[0, 3.1]` 하나뿐). 사이클이 20초로 길어지면(변형 2) 사이클마다 약 5.2초씩 꺼진다 —
> 게이팅이 죽은 게 아니라 사이클 길이가 정하는 값이다.

### 변형 1 (앱솔루트 페어) 차이

| 항목 | 표준 | 변형 1 |
|---|---|---|
| #5 수령자 | 은화 1명 | 은화 + 엠마 |
| #8·#9 | 무발동 | 상시 활성(아군 전체 `armor_break_dmg` +30.97) |
| 은화가 받는 ⑤ | 42.24 | 42.24 + 30.97(자기 #9) + 30.97(엠마 #6) = **104.18** |
| 적 `received_dmg` | 27.87(10초) | + 환경 조성 3.9 → 버스트 시 **7.8** 상시 |
| 은화 ② | `atk_pct` 42.24 | + 엠마 `전장 조성` 시전자 기준 40.07(10초) |
| 은화 ③ | `crit_rate` +8.16 | + 엠마 `crit_dmg` +23.51 |

## 스킬 간 상호작용

- **`포메이션 AS` ↔ `포메이션 LT` 양방향 순환** — 엠마 시나리오와 같은 사슬이다.
  #5가 `allies_squad`라 엠마에게 `포메이션 AS` 상태가 붙고, 그 대가로 엠마의 `포메이션 LT`가
  은화에게 붙어 #8·#9가 열린다. **첫 항목이 상태명의 담체**여야 하고(`PARSING.md` Step 7),
  대상을 넓히거나 이름 번호를 밀면 6종이 통째로 죽는다.
  `runner/doclint.py` 검사 K는 이 **교차 담체**를 이제 정상으로 인정한다(2026-09-10 확장).

- **카모플라쥬 → 방어력 무시 전환 → 방어력 무시 대미지 3단**
  - #0/#1이 상태를 부여 → #3의 `self_state:카모플라쥬`가 참 → 일반 공격이
    `is_armor_break_damage`가 되고, 같은 조건의 #4(+42.24)가 **그 히트에만** 실린다.
  - `stealth` 자체는 미구현(❌)이지만 **상태 판정은 이름으로** 하므로 사슬은 성립한다.
  - #1(`full_charge_fire` + `during_full_burst`)이 풀버스트 안에서 매 발 갱신하므로
    풀버스트 내내 끊기지 않고, 밖에서는 버스트 시전분 5초가 전부다.
  - 페어에서는 여기에 #9(자기 아군 버프)와 엠마 #6이 합산돼 ⑤가 104.18까지 간다.

- **모드 사격의 방어력 무시는 부속 버프로 표현한다**
  - 원문 `대미지 : 최종 공격력 105.6% 방어력 무시 대미지`. `weapon_change` 스키마에
    방어력 무시 필드가 없어 #11(`armor_break_enabled`)로 분리했고, 모드가 끝나도 자동으로
    사라지지 않으므로 #12가 `event:state_end:내부 폭발탄`에서 지운다(`PARSING.md` §7-13).
  - 실제로는 같은 프레임에 #0이 붙어 #3이 이미 켜져 있다 — #11은 **중복이지만 원문의
    성질**이라 남긴다. 대신 #12가 없으면 모드가 끝난 뒤에도 영구 방어력 무시가 된다.

- **모드는 1발이라 즉시 끝난다**
  - `duration` 문구 없이 `최대 장탄 수 : 1발`뿐 → `duration_bullets: 1`. 빼면 영구 모드다.
  - `(사용 무기 변경 시 최대 장탄 수 효과 갱신)` 괄호구가 **없어** 오버로드·큐브의 최대 장탄이
    얹히지 않는다 → 1발 고정. 종료 시 SR은 **만탄 복구**, 차지는 처음부터 다시.
  - 기본 무기가 SR이라 **모드가 RL이어도 `projectile_explosion_dmg_pct`를 못 받는다**
    (GAMEPLAY §무기 메카닉 표에 이미 등재). 본인이 뿌리는 #8을 정작 본인은 못 쓴다.

- **#13은 자기 발의 대미지를 키우지 못한다**
  - `hit_count:1`은 그 발의 `calc_damage` **뒤**에 오는 트리거다(`_BULLET_BOUND_TIMINGS` 규약).
    폭발탄 자신은 27.87을 못 받고, 그 뒤 10초 동안의 스쿼드 딜이 받는다.
  - 모드 사격은 `weapon_hit:` 이벤트를 쏘지 않으므로 `hit_count:1` + `self_state:[모드명]`이
    유일한 표현이다(선례: 라플라스 `라플라스 버스터 4`). 발사·명중이 모드 종료 **앞**에서
    처리되므로(`_tick_weapon_change`) 조건이 성립한다 — 순서가 바뀌면 이 디버프가 영구 무발동이다.

## 유저 확인 사항

- **Q1. 재장전 후 첫 발사 딜레이(post_reload_delay)** (2026-09-10 유저 확인)
  **A: 기본값 0으로 진행.** `_exceptions`의 SR/RL 예외 3명은 전부 `DOWN_Charge`이고
  `UP` 입력 SR(홍련 : 흑영·벨벳·맥스웰 등)은 전원 기본값 0이다. 발사 후 딜레이는 CDN 유도값
  `0.22 + max(0.16, 0) = 0.38초`.

- **Q2. 변경 무기 유형** (문서에 답이 있음 — 질문 아님)
  **A**: `GAMEPLAY.md` §무기 메카닉의 「무기 변경 중에도 무기 타입은 기본 무기로 따진다」 표가
  **은화 : 택티컬 업 = 기본 SR / 모드 RL**로 이미 기록하고 있다(원문 `폭발탄을 발사해 넓은
  범위를 공격합니다`와 일치). RL은 무기군 기본이 차지형이라 `charge` 필드는 붙이지 않는다.

- **Q3. 같은 이름 `카모플라쥬`를 두 clause가 부여한다** (자체 판단 — 선례 있음)
  **A (자체 판단, 선례: 마나 `매터 시그마`·로산나 `은신`·레이븐 `급소 공략`)**: 트리거·조건이
  달라 `timing` 배열로 합칠 수 없고(합치면 `during_full_burst`가 버스트 시전분까지 막는다),
  이름이 `self_state:카모플라쥬`로 참조되므로 **두 항목 모두 같은 이름**으로 둔다.

- **Q4. `내부 폭발탄` 이름이 모드(자신)와 디버프(적) 양쪽에 쓰인다** (자체 판단)
  **A (자체 판단)**: 원문이 둘 다 그 이름으로 부르고, 자신 쪽은 `self_state:`(모드 게이팅),
  적 쪽은 `target_state:`(베스티 : 택티컬 업이 읽을 예정)로 참조된다. `_has_self_state`는
  `target_chars`에 시전자가 있는지를 보고 모드는 `_active`가 아니라 `state["weapon_change"]`에
  있으므로 **서로 오검출되지 않는다** → 둘 다 같은 이름(PARSING Step 7 예외).

- **Q5. 이 캐릭터에서 승격한 일반 규칙?** **없음.**

## 해석 선언

| 효과 | 해석 | 근거 | 대안 해석이었다면 |
|---|---|---|---|
| #5 대상 | `allies_squad`(신규, 시전자 포함) | 원문 `동일 스쿼드 아군 전체에게` · condition `squad_ally_exists`와 같은 기준 | 미구현/빈 리스트면 본인 `crit_rate`도 0이고 **엠마 쪽 효과 6종까지 죽는다**. `all_allies`로 넓히면 더미까지 `포메이션 AS` 상태를 갖는다 |
| #0·#1 이름 | 같은 이름 `카모플라쥬`를 **중복**시킨다 | `_by_name()` 정확 일치 · PARSING Step 7 예외 | ` 2`를 붙이면 풀차지 갱신분이 `self_state` 판정에서 빠져 **풀버스트 내내 ⑤가 꺼진다**(#3·#4 = 42.24 손실) |
| #3·#4 조건 | `passive` + `self_state:카모플라쥬`(런타임 재평가) | `self_state`는 `_RUNTIME_COND_PREFIXES` 소속 | 지속시간으로 흉내 내면 상태와 어긋난다 — 5초 buff로 복사하면 갱신 경로가 둘이라 만료 시각이 꼬인다 |
| #10 무기 유형 | **RL** | `GAMEPLAY.md` §무기 메카닉 표(기본 SR / 모드 RL) · 원문의 광역 폭발탄 서술 | SR로 두면 무기군 기본 딜레이·차지 규약이 달라지고, 「기본 무기로 따진다」 표와 어긋난다 |
| #10 종료 | `duration_bullets: 1` (지속시간 없음) | 원문에 유지·해제 문구 없이 `최대 장탄 수 : 1발`뿐 (`PARSING.md` §7-13, 츠바이·맥스웰 선례) | 빼면 **영구 모드** — 전투 내내 RL 1발 모드로 서서 SR 딜이 통째로 사라진다 |
| #10 장탄 버프 | 오버로드·큐브 최대 장탄 **미적용**(`max_ammo_buff_applies` 미설정) | 원문에 `(사용 무기 변경 시 …)` 괄호구 없음 | 적용하면 기본 스펙 오버로드만으로 1발 → 3발이 되어 모드 딜 3배 |
| #11 방어력 무시 | 모드 종속 **부속 버프**(`armor_break_enabled`) + #12로 해제 | weapon_change 스키마에 방어력 무시 필드 없음 · 드레이크 : 그레이트 빌런 `오버 오버 드라이브 3` 선례 | #12가 없으면 모드 종료 후에도 **영구 방어력 무시** — 비카모플라쥬 구간의 일반 공격이 전부 방무가 되어 딜이 부풀려진다 |
| #13 트리거 | `hit_count:1` + `self_state:내부 폭발탄` | 모드 사격은 `weapon_hit:`을 쏘지 않는다(GAMEPLAY §무기 메카닉) · 라플라스 `라플라스 버스터 4` 선례 | `burst_cast`로 옮기면 명중과 무관하게 붙어 원문(`명중 대상에게`)과 어긋나고, 모드가 빗나가는 모델이 생기면 틀린다 |
| #6 | 평문 `charge_dmg_pct`(④에 **가산**) | GAMEPLAY §차지 배율은 가산이다 | `charge_dmg_mag_pct`(배율)로 잡으면 250% 무기 기준 기여가 +41.81pt가 아니라 +104.5pt — 차지 아군 딜이 통째로 부풀려진다 |

## impl/bug-fix용 검증 체크리스트

> 판정: 2026-09-10 단계 4 Phase D. seed=1, 180초. **11/11 PASS**(1건은 실측에 맞춰 기대를 수정).
> 표준 총딜 **1,461,090,530**(은화 200,631,134) · 변형 1 **1,925,133,233**(은화 **339,754,513**,
> 표준 대비 **+69.4%**) · 변형 2 **1,569,832,777**. 회귀 하네스 **33/33 PASS**.

- [x] 표준: #8·#9 **미적용**(네거티브) — PASS (`projectile_explosion_dmg` 0.00 · `armor_break_dmg_pct` 42.24 = #4 단독. 변형 1에서는 각각 10.52 · 104.18)
- [x] 표준: #5 `crit_rate` 수령자가 **은화 1명**(`crit_rate` 0.150 → 0.232) / 변형 1에서는 {엠마, 은화} 둘 다 0.232 — PASS
- [x] #0이 버스트마다 1회, #1이 풀버스트 중 풀차지마다 갱신 — PASS (부여 **129회** / 버스트 15회)
- [x] #3·#4의 `self_state:카모플라쥬` 게이팅이 실제로 꺼진다 — PASS. **다만 12.55초 사이클에서는 꺼지는 구간이 없다**: `armor_break_enabled` OFF 구간이 표준·변형 1 모두 `[0.0, 3.1]` 하나뿐이다. 풀버스트 마지막 풀차지 +5초와 다음 버스트 시전(12.53초 뒤)이 겹쳐 끊기지 않기 때문이고, **버쿨감 없는 20초 사이클(변형 2)에서는 사이클마다 약 5.2초씩 꺼진다** — `[0,3.1] [17.8,23.1] [37.9,43.1] [57.9,63.1] …`. 초안의 "비풀버스트 구간에는 꺼진다"는 사이클 길이에 달린 이야기라 실측에 맞춰 고쳤다
- [x] #10이 사이클당 정확히 **1발** 발사하고 즉시 종료, SR 만탄 복귀, 재장전 미삽입 — PASS
      (t=15.633 모드 진입 → **16.250 1발**(dmg 2,024,103, 평시 풀차지 1,429,304) → 같은 프레임에 모드 종료 → 17.283 다음 발에서 잔탄 **13** = 만탄 14에서 1발. 표준·변형 1의 은화 **재장전 로그 0건**)
- [x] #10 모드 장탄이 오버로드 최대 장탄 버프를 **받지 않을 것** — PASS (모드 1발 고정. 기본 무기 쪽은 6 → **14**로 오버로드가 붙는다)
- [x] 모드 종료 후 #11이 남아 있지 않을 것 — PASS (`내부 폭발탄 2` 활성 15회 ↔ `내부 폭발탄 3` 제거 instant 29회. 29 = 모드 종료 15 + **디버프 만료 14** — 같은 이름을 두 항목이 쓰므로 `event:state_end:내부 폭발탄`이 양쪽에서 뜬다. 두 번째는 이미 지워진 버프를 지우는 무해한 no-op이지만, 이 이름에 다른 효과를 물릴 때는 두 번 뜬다는 걸 알고 붙여야 한다)
- [x] #13이 사이클당 1회, 적에게 10초 — PASS (activate 15회, 수령자 `__enemy__`, 적 `received_dmg` 32.07 = 27.87 + 외부 4.2 → 10초 뒤 4.20). 폭발탄 자신은 못 받는다(`hit_count:1`이 그 발의 계산 뒤)
- [x] #6 `charge_dmg_pct`가 ④에 퍼센트포인트 가산 — PASS (`charge_dmg_pct` 41.81 관측)
- [x] #8은 본인에게 무효(기본 무기 SR) — PASS (은화 `projectile_explosion_dmg` 0.00 / 변형 2의 홍련 : 흑영만 **10.52**)
- [x] 사이클 간격 **12.53 균일**(7.48 계열) · 본인 `burst_cast` 사이클당 1회(15/15) — PASS

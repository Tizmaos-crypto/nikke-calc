# 목단 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B1
- 본인 쿨다운: 40s → **애장 1단계 `열혈`이 −20s** → 실효 20s → 아군 쿨감까지 얹혀 매 사이클
- 무기 유형: AR (60발, 재장전 1.5s, 연사 12발/s, 계수 14.71%, 코어 200%)
  - 버스트 중에는 **SMG로 변경** (연사 24발/s, 계수 14.7%, **장탄 무한**, 10초)
- 속성 코드: 전격 · 클래스: 방어형 · 기업: 테트라
- 애장품: **있음 — 모란꽃 모양 비녀** (`favorite_slots: [1, 2, 3]` → 단계 N이 스킬 N을 교체)
- 작성 사이클 수: 1 (애장 3단계 기준 매 사이클 버스트)

## 효과 요약 (보강: 효과 단위 stat 키)

기본 스펙은 **애장품 3단계**라 세 슬롯 전부 애장 판본이다. 아래는 그 상태 기준.

| ID (parsed name) | 스킬 | 판본 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 다 덤벼! | 스킬1 | 공통 | `battle_start` | `def_pct`: 3.51% (`scaling: lost_hp_pct`) | self | -1 |
| 다 덤벼! 2 | 스킬1 | 공통 | `hit_count:5` + `self_state:무기 변경` | `bonus_damage`: 47.18% | `target` | — |
| 열혈 | 스킬1 | **애장1 전용** | `event:enemy_spawn` | `burst_cooldown`: 20.0s | self | -1 |
| 여긴 내가 맡는다! | 스킬2 | 공통 | `last_bullet_fire` | `taunt` | `enemies_top_atk:3` | 4s |
| 근성 1 / 2 / 3 | 스킬2 | 공통 | `hp_below_count:20:1` / `:2` / `:3` (+ `max_trigger: 1`) | `max_hp_pct`: 91.0 / 69.84 / 51.09% | self | 3s |
| 여긴 내가 맡는다! 2 | 스킬2 | **애장2 전용** | `full_burst_start` + `self_state:열혈` | `burst_cooldown_reduce`: 7.48s | `all_allies` | — |
| 정정당당 승부다! | 스킬3 | 공통 | `burst_cast` | `weapon_change` (SMG · 계수 14.7% · **장탄 −1(무한)**) | self | 10s |
| 정정당당 승부다! 2 | 스킬3 | 공통 | `burst_cast` | `lifesteal_pct`: 36.14% | self | 10s |
| 주목 | 스킬3 | 공통 | `burst_cast` | `taunt` | `all_enemies` | 10s |
| 정정당당 승부다! 3 | 스킬3 | 공통 | `burst_cast` | `received_dmg_pct`: −35.14% | `all_allies` | 10s |
| 정정당당 승부다! 4 | 스킬3 | 공통 | `burst_cast` | `def_caster_based_pct`: 14.85% | `all_allies` | 10s |
| **정정당당 승부다! 5** | 스킬3 | **애장3 전용** | `burst_cast` | `atk_caster_based_pct`: 42.57% | `all_allies` | 10s |
| 정정당당 승부다! 6 | 스킬3 | 공통(비애장) | `passive` + `self_state:정정당당 승부다!` | `cover_disabled` | self | -1 |

### `self_state:무기 변경`은 총칭 상태명이다

원문이 "자신이 **무기 변경 상태**라면"이라고 모드 이름 대신 총칭을 쓴다. 그래서 데이터도
원문 그대로 `self_state:무기 변경`을 쓰고, **코드가 그 총칭을 읽는다** —
`buff_manager.WEAPON_CHANGE_STATE` + `_has_self_state()`가 "이 캐릭터가 무슨 모드든 들고
있는가"로 판정한다. 모드명(`정정당당 승부다!`)으로 대조하면 영원히 거짓이 되기 때문이다.
`runner/doclint.py` K 항목도 이 케이스를 예외로 통과시킨다.

**총칭 판정은 이 캐릭터에서만 쓰이고, 두 번 어긋났던 자리다.** 처음에는 모드 *이름*으로만
대조해 영구 거짓이었고(발동 0회), 총칭 분기를 넣은 뒤에는 `weapon_change_name(caster)
is not None`으로 보는 바람에 — 이 함수는 모드가 없을 때 `None`이 아니라 **빈 문자열**을
준다 — 영구 참으로 뒤집혀 모드 밖 일반 공격에도 통과했다(180초 781회, 그중 83회가 모드 밖).
`bool(...)`로 보는 게 맞고, 지금은 **모드 밖 발동 0회**다.

### 딜에 실리는 것과 안 실리는 것

| stat | 상태 | 결과 |
|---|---|---|
| **`bonus_damage`** (다 덤벼! 2) | ✅ | **본인 딜의 약 38%.** 버스트 SMG 240발 → `hit_count:5` 48회/사이클 |
| **`atk_caster_based_pct`** (정정당당 승부다! 5) | ✅ factor② | **애장 3단계 전용. 스쿼드 기여의 본체** |
| `def_pct`(다 덤벼!) | ⚠️ 아군 대상은 timeline 미반영 | 딜 무관 |
| `taunt`(여긴 내가 맡는다! · 주목) | ✅ 보스 공격 대상만 바꾼다(`여긴 내가 맡는다!`는 적에게 걸려 목단이 도발자) | 딜 무관 |
| `lifesteal_pct` | ✅ 회복 | 딜 무관 |
| `received_dmg_pct`(아군) · `def_caster_based_pct` | ⑥ / ⚠️ | 생존 전용 |
| `max_hp_pct`(근성) | ⚠️ `hp_below:N` 이벤트 발생처 없음 | **영구 무발동** |
| `cover_disabled` | ❌ 미구현 | 엄폐 컨트롤 미연동 |

## 검증 스쿼드
- **격리 (B1 · 쿨 40s → 보조 B1 필요, 5슬롯)**:
  `["목단", "리틀 머메이드", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 조건부 효과 스캔: `back_row`·`no_burstN_ally`·코드/클래스 조건 **없음**.
  - **애장 1단계 이상에서는 목단이 매 사이클 B1을 채워 리틀 머메이드가 거의 버스트하지 않는다.**
    보조 B1으로 넣은 자리인데 실제로는 쿨감 공급자(`버블 오더 2`)로만 쓰인다.
  - 애장품 단계 대조(`--favorite "목단:N"`)를 여기서 낸다.
- **실전 조합 (하네스)** — 목단은 하네스 5개 스쿼드에 들어 있다. 커버 범위가 넓어
  회귀 변동을 읽기 좋은 자리다:

  | 스쿼드 | 멤버 | 적 |
  |---|---|---|
  | `S36_서머메이든` | 아니스 : 스파클링 서머 · 에이드 : 에이전트 바니 · **목단** · 메이든 : 아이스 로즈 · 프리바티 | 수냉 |
  | `S38_사쿠라로산나` | **목단** · 사쿠라 : 블룸 인 서머 · 맥스웰 · 아크레인저 블랙 · 로산나 : 시크 오션 | 철갑 · 파츠 |
  | `S39_라플라스리코리코` | **목단** · 치사토 · 라플라스 · 에이다 · 타키나 | 전격 · 파츠 |
  | `S40_민트디젤` | **목단** · 민트 · 스노우 화이트 : 헤비암즈 · 프리카 · 디젤 : 윈터 스위츠 | 풍압 · 파츠 |
  | `S40_플로라` | 디젤 : 윈터 스위츠 · 플로라 · 브리드 : 사일런트 트랙 · 스노우 화이트 : 헤비암즈 · **목단** | 풍압 |

### 애장품 단계별 실측 (180s, `--expected`, 격리 스쿼드)

| 단계 | 스쿼드 총딜 | 직전 대비 | 목단 딜 | 무엇이 바뀌나 |
|---|---|---|---|---|
| 0 | 1,899,643,009 | — | 175,258,364 | 목단 쿨 40s → 격 사이클, B1 절반은 리틀 머메이드 |
| 1 | 1,916,414,063 | +0.88% | 295,880,987 | `열혈`(쿨 −20s) → 목단이 매 사이클 B1을 가져가고 **무기 변경 가동률이 2배** |
| 2 | 1,936,395,056 | +1.04% | 327,985,886 | `여긴 내가 맡는다! 2`(아군 쿨감 7.48s) |
| 3 | **2,290,533,102** | **+18.29%** | **407,150,384** | **`정정당당 승부다! 5`(`atk_caster_based_pct` 42.57%, 전체)** |

**스쿼드 총딜과 목단 본인 딜이 서로 다른 이야기를 한다.** 애장 1단계는 목단 본인 딜을
+68.8% 올리지만(175.3M → 295.9M) 스쿼드 총딜은 +0.88%밖에 안 오른다 — 목단이 B1을 독점하면서
더 잘 때리는 리틀 머메이드의 버스트를 잡아먹기 때문이다. **3단계의 도약(+18.3%)이 본체다.**

실측 (180s, 애장3): 스쿼드 총딜 **2,290,533,102** / 목단 407,150,384 (17.8%).

## 사이클 타임라인 (애장 3단계 기준)

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0 | battle_start | 다 덤벼!(`def_pct`, 잃은 체력 비례) | self / 영구 | 딜 무관 |
| 0 | `event:enemy_spawn` | **열혈**(`burst_cooldown` 20.0s) | self / 영구 | 쿨 40 → **20** |
| 0~3 | AR 자동 사격 (0.083s 간격) | 탄창 끝에 여긴 내가 맡는다!(도발) | 적 3기 / 4s | 딜 무관. **다 덤벼! 2는 여기서 안 터진다** |
| **3.00** | **목단 burst_cast** | 정정당당 승부다!(**무기 변경 SMG, 장탄 무한**) · 2(`lifesteal`) · 주목(도발) · 3(`received_dmg`) · 4(`def_caster_based`) · **5(`atk_caster_based_pct` +42.57, 전체)** | self / all_allies / 10s | 사격 간격 0.083s → **0.033/0.05 교대(평균 0.042s)** |
| 3.133 | SMG 5번째 히트 | **다 덤벼! 2** 첫 발동 | 대상 | 모드 진입 뒤 첫 5히트 |
| 3.25 | **full_burst 시작** | 여긴 내가 맡는다! 2(`burst_cooldown_reduce` 7.48s, 전체) | all_allies | `self_state:열혈` 통과 |
| 3.00~13.00 | SMG 무한 사격 | **240발** (재장전 없음) + 다 덤벼! 2 5히트마다 | 대상 | 버스트 구간에서 **48회** |
| 13.00 | 무기 변경 종료 | SMG → AR 복귀, 버스트 자버프 전부 만료 | | |
| 13.25 | full_burst 종료 | | | |
| 15.25 | 목단 burst_cast (2회째) | 위 반복 | | 쿨 20 − (7.48 자기 + 7.48 리틀 머메이드) → 하한 |
| 15.50 | full_burst 시작 (2회째) | | | **사이클 12.25s** |

사이클 간격 실측: 3.25 → 15.5 → 27.75 → 40.0 → … **12.25s 등간격**(표준 하한).
목단 버스트: 3.0 / 15.25 / 27.5 / 39.75 / … — 매 사이클.

사이클당 목단 히트 실측 **267회**(일반공격) = 버스트 SMG 240발 + 비버스트 AR 27발.
`다 덤벼! 2`는 사이클당 **48회**(= 240÷5, 전량 무기 변경 구간 안), 180초 전체 **698회**.
**무기 변경 밖 발동은 0회다.**

사이클 딜 구성 실측(사이클 2, 합 29.0M): 일반공격 17.96M(267히트, 62.0%) ·
**다 덤벼! 2 11.01M(48히트, 38.0%)**.

## 스킬 간 상호작용

- **`열혈`(애장1) → `여긴 내가 맡는다! 2`(애장2) — 이름 게이팅**
  - 경로: 열혈(`event:enemy_spawn`, 영구) → `self_state:열혈` → 애장2의 아군 전체 쿨감이 열린다.
  - 애장 1단계가 없으면 애장 2단계 효과도 통째로 죽는다(츠바이의 `관통학 개론`과 같은 구조).
  - `event:enemy_spawn`은 단일 보스 시뮬에서 **battle_start 1회**로 발생한다
    (`docs/GAMEPLAY.md` §트리거 발동 의미).
- **`열혈`(쿨 −20s) → 무기 변경 가동률 2배 → `다 덤벼! 2` 발동 횟수 증가**
  - 목단의 딜은 무기 변경 구간에 몰려 있다(SMG 24발/s + 5히트마다 추가 대미지).
    쿨 −20s는 그 구간이 전체 시간에서 차지하는 비율을 두 배로 만든다.
  - 반대 방향의 부작용도 같이 온다 — 목단이 B1 슬롯을 독점해 **동료 B1의 버스트가 사라진다.**
    애장 1단계에서 목단 본인 딜 +68.8%에 스쿼드 총딜 +0.88%인 것이 그 상쇄다.
- **`정정당당 승부다!`(무기 변경) → `다 덤벼! 2`(추가 대미지)**
  - 경로: weapon_change(SMG 24발/s, 10초) → `self_state:무기 변경` 참 →
    `hit_count:5`가 240발 동안 48회 → `bonus_damage` 47.18% × 48.
  - 모드의 **존재 자체**가 게이팅 키다(모드 이름이 아니라). 이 판정이 위에서 두 번 어긋났던 자리다.
  - **동일 timing(`burst_cast`) 배열 순서**는 무기 변경 → 자버프 → 아군 버프 순으로 유지한다.
- **`장탄 무한` → `last_bullet_fire` 소멸**
  - 무기 변경 `max_ammo: -1`이라 버스트 10초 동안 마지막 탄환이 오지 않는다
    → `여긴 내가 맡는다!`(도발)는 **비버스트 구간에서만** 발동한다. 딜 무관이라 결과에는 영향 없음.

## 유저 확인 사항
- 초안 단계 없음. 아래는 파싱·코드·실측에서 확인한 사실.
- Q: `열혈`의 `burst_cooldown: 20.0`은 증가인가 감소인가?
  A: **감소**. `_cast_burst()`가 `cd = max(0, cd − cd_buff)`로 뺀다. 40 → 20s.
- Q: `다 덤벼! 2`의 condition을 모드 이름(`정정당당 승부다!`)으로 바꿔야 하나?
  A: **아니다.** 원문이 총칭("무기 변경 상태")을 쓰므로 데이터는 원문을 따르고,
  코드의 `WEAPON_CHANGE_STATE` 경로가 그것을 읽는다. 고칠 일이 생기면 데이터가 아니라
  그 경로를 고친다.
- Q: `다 덤벼! 2`는 얼마나 터지나?
  A: 무기 변경 구간에서 SMG 240발 → `hit_count:5` **48회/사이클**, 180초 총 **698회**.
  모드 밖에서는 0회다.
- Q: `근성 1/2/3`은?
  A: **영구 무발동.** `hp_below_count:N:순서`는 `_timing_match`에 분기만 있고
  `hp_below:N` 이벤트 발생처가 없다(`docs/IMPL-STATUS.md`).
- Q: 무기 변경 SMG의 연사 속도는?
  A: `weapon_delays.json`에 목단 항목이 없어 **SMG 기본값 24발/s**를 쓴다.
  실측 사격 간격 0.033/0.05 교대(평균 0.042s)로 확인.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 열혈 | 버스트 쿨 **−20초** (40 → 20) | `_cast_burst()`의 `cd − cd_buff` · 실측 사이클 12.25s | 증가였다면 목단이 사실상 버스트를 못 쓴다 |
| **다 덤벼! 2** | 원문의 총칭 "무기 변경 상태"를 그대로 데이터에 두고 코드가 해석 | `WEAPON_CHANGE_STATE` · `_has_self_state()` · `doclint` K 예외 · 실측 모드 밖 0회 | 모드명 `정정당당 승부다!`로 적었다면 결과는 같지만 원문과 어긋난다. 총칭을 **정확 일치**로만 대조하면 0회, **빈 문자열을 참으로 읽으면** 모드 밖까지 781회 — 양쪽으로 다 틀려 본 자리다 |
| 정정당당 승부다! (무기 변경) | SMG 24발/s · 장탄 무한 · 10초 | `weapon_type: SMG` + `max_ammo: -1` · 실측 240발/버스트 | 장탄 유한이면 재장전으로 발수가 줄고 `last_bullet_fire` 도발이 버스트 중에도 돈다 |
| 정정당당 승부다! 5 (애장3) | 아군 전체 `atk_caster_based_pct` = 목단 ATK × 42.57%를 각자 `atk_flat`에 가산 | `get_buffs()` 후처리 · 실측 총딜 +18.20% | `atk_pct`(수령자 공격력 %)였다면 방어형 목단의 낮은 공격력 기준이 아니게 되어 값이 크게 달라진다 |
| 근성 1/2/3 | 무발동 | `hp_below:N` notify 없음 | 발동해도 `max_hp_pct` 3초라 딜 무관 |
| 여긴 내가 맡는다! · 주목 | 도발, 딜 무관 | `taunt` ✅ | 보스 공격 패턴(`enemy.patterns`의 `attack`)이 있으면 목단이 고르는 공격을 끌어 아군 생존이 바뀐다 (딜에는 여전히 무관) |

## impl/bug-fix용 검증 체크리스트
- [ ] `battle_start` 직후 `열혈` 활성 → 목단 첫 버스트 쿨이 **20s**로 잡히는지 (애장 1단계 이상)
- [ ] 풀버스트 진입 시 활성(애장3): 정정당당 승부다! 2·3·4·**5** + 주목 + 무기 변경 모드
- [ ] 버스트 중 사격 간격이 0.083s(AR) → **0.033/0.05 교대(SMG 24발/s)** 로 바뀌고 재장전이 없는지
- [ ] 사이클당 목단 일반공격 **267회** (버스트 SMG 240 + 비버스트 AR 27)
- [ ] **`다 덤벼! 2` 사이클당 48회 / 180초 698회** — 전량 무기 변경 구간 안,
      **구간 밖 0회**(`_has_self_state`의 총칭 분기가 빈 문자열을 거짓으로 읽는지)
- [ ] 사이클 간격 **12.25s** (표준 하한) / 애장 0단계에서는 목단이 격 사이클로 버스트
- [ ] 애장 단계별 스쿼드 총딜: 0 → 1(+0.88%) → 2(+1.04%) → 3(**+18.29%**)
- [ ] 발동 금지: `근성 1/2/3` — 0회 (`hp_below` 이벤트 없음)
- [ ] 버스트 중 `여긴 내가 맡는다!`(`last_bullet_fire`) 0회 — 장탄 무한이라 마지막 탄환이 없음

# 솔린 : 프로스트 티켓 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계 없음. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B1
- 본인 쿨다운: 40s (안전하게 모실게!)
- 무기 유형: SG (9발, 재장전 1.5s, 연사 1.5발/s, 계수 201.5%, **펠릿 10 × 총구 1**, 코어 200%)
- 속성 코드: 수냉 · 클래스: 지원형 · 기업: 엘리시온
- 애장품: **없음**
- 작성 사이클 수: 2 (쿨 40s → 본인 버스트 사이클 + 미사용 사이클)

## 핵심 전제

**킷의 절반이 힐·생존 계열이라 보스 DPS 시뮬에서는 죽어 있다.** 딜에 실제로 남는 것은 둘뿐이다:

1. `버스트 스킬 재사용 시간 감소` — 풀버스트마다 아군 전체 버스트 쿨 −7.48s
2. 솔린 본인의 SG 사격 (실측 스쿼드 딜의 9.9%)

`티켓 효과`(`hp_only_caster_based_pct`)는 최대 체력만 올리므로, **체력을 참조하는 아군이 없으면 딜 0**이다.
`열차 탑승 도와줄게!`·`안전하게 모실게!`(회복)는 전부 딜 무관.

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 티켓 | 스킬1 | `battle_start`, `burst_cast` | `gauge_charge` +1 (`gauge_id: 티켓`, `gauge_max: 2`) | `all_allies` | — (instant) |
| 티켓 효과 | 스킬1 | `passive` + `gauge_above:티켓:1` | `hp_only_caster_based_pct`: 10.0% × `scaling: stack_count` (`scaling_ref: 티켓`) | `all_allies` | -1 (영구) |
| 버스트 스킬 재사용 시간 감소 | 스킬1 | `full_burst_start` | `burst_cooldown`: 7.48s | `all_allies` | -1 (영구 buff) |
| 첫차 할인 해제 | 스킬1 | `full_burst_start` | `remove_named_buff` → `첫차 할인` | `all_allies` | — (instant) |
| 열차 탑승 도와줄게! | 스킬2 | `event:ally_hp_below:15` + `target_state:티켓` | `heal_hp_pct`: 12.27% (`scaling: max_hp`) | `all_allies` | — (instant) |
| 열차 탑승 도와줄게! 2 | 스킬2 | `event:ally_hp_below:15` + `target_state:티켓` | `gauge_consume` −1 (`티켓`) | `all_allies` | — (instant) |
| 안전하게 모실게! | 스킬3 | `burst_cast` | `heal_hp_pct`: 32.26% (`scaling: max_hp`) | `all_allies` | — (instant) |

### 파싱 갭 (딜 무관하지만 기록해 둔다)

1. **`첫차 할인` 버프 자체가 파싱돼 있지 않다.** 원문 스킬2에 `■ 전투 시작 시 아군 전체에게
   [첫차 할인] [6초 유지]`가 있는데, `parsed_skills.json`에는 그것을 **제거하는**
   `첫차 할인 해제`만 있고 **부여하는 항목이 없다.** 실동작은 "없는 것을 지우는" 무해한
   no-op이다. 원래 역할은 초반 6초 동안 회복이 티켓을 소모하지 않게 하는 면제권인데,
   회복 경로 자체가 죽어 있어 결과가 같다.
2. **`열차 탑승 도와줄게!`는 기본 경로에서 무발동.** `event:ally_hp_below:N`은 ✅이지만 체력을 15% 밑으로
   끌어내리는 경로가 보스 공격 패턴(`enemy.patterns`의 `attack`)뿐이라, 패턴이 없으면 티켓이 **소모되는 일이 없다**(2026-09-14 갱신).

## 검증 스쿼드
- **표준 (B1 · 쿨 40s → 보조 B1 필요, 5슬롯)**:
  `["솔린 : 프로스트 티켓", "리틀 머메이드", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 조건부 효과 스캔: `no_burstN_ally` · `back_row` · `allies_weapon/class/code` 류 조건 **없음**.
    `target_state:티켓`은 자기 킷이 만든 게이지라 자기완결 → **표준 1개로 충분**.
  - 솔린은 쿨 40s라 **격 사이클로만** B1을 채운다. 나머지 사이클은 리틀 머메이드가 맡는다.
- **쿨감 격리 대조 (선택)**: `["솔린 : 프로스트 티켓", "test_B2", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 다른 쿨감 공급자를 뺀 상태에서 `burst_cooldown` 7.48s가 **풀버스트당 정확히 1회**만
    적용되는지 확인용. 실측 버스트 간격 **32.53s**(= 40 − 7.48)로 이중 적용이 없음을 확인했다.

- **실전 조합 (하네스)** — 솔린은 SG 다수 덱의 고정 자리다:

  | 스쿼드 | 멤버 | 적 · 비고 |
  |---|---|---|
  | `S40_토브드레이크` | 토브 · 아르카나 : 포츈 메이트 · 도로시 : 세렌디피티 · 드레이크 · **솔린** | 풍압 · 코어 52px · **`no_burst_char: 솔린`** |
  | `S38_누아르` | 토브 · 아르카나 : 포츈 메이트 · 도로시 : 세렌디피티 · 누아르 · **솔린** | 철갑 · SG 적정거리 |
  | `커버_레오나슈가` | 토브 · 레오나 · 도로시 : 세렌디피티 · 슈가 · **솔린** | 수냉 |

  세 조합 모두 B1 자리에 **토브**가 있다. 특히 `S40_토브드레이크`는 실전 운용대로
  **솔린이 버스트를 아예 쓰지 않는다**(`no_burst_char`) — 그러면 `안전하게 모실게!`(회복)와
  `티켓` 2장째 충전이 모두 사라지고, 남는 것은 `full_burst_start`의 아군 쿨감뿐이다.
  **솔린의 실전 가치가 버스트가 아니라 상시 쿨감에 있다는 것을 이 설정이 말해 준다.**

실측 (180s, `--expected`, 표준 스쿼드): 스쿼드 총딜 **1,979,010,072** /
솔린 195,921,440 (9.9%, 전량 기본 공격 2640히트).

## 사이클 타임라인

솔린 쿨 40s + 자체 쿨감 → **2사이클마다 1회** 버스트. 아래는 2사이클 한 벌.

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0 | battle_start | 티켓(`gauge_charge` +1 → **티켓 1**) | all_allies | 티켓 효과 = 10% × 1 |
| 0 | (passive) | 티켓 효과(`hp_only_caster_based_pct` 10%) | all_allies / 영구 | `gauge_above:티켓:1` 참 |
| 3.00 | **솔린 burst_cast (1회째)** | 티켓(+1 → **티켓 2, 캡 도달**) · 안전하게 모실게!(회복) | all_allies | 티켓 효과 = 10% × 2 = **20%** |
| 3.25 | **full_burst 시작 (사이클 1)** | 버스트 스킬 재사용 시간 감소(`burst_cooldown` 7.48s) · 첫차 할인 해제(no-op) | all_allies / 영구 | 솔린 쿨 40 → 실효 감소 |
| 13.25 | full_burst 종료 | — | | |
| 15.25 | B1 **리틀 머메이드** burst_cast | (솔린은 쿨 중) | | 솔린 미사용 사이클 |
| 15.50 | full_burst 시작 (사이클 2) | 버스트 스킬 재사용 시간 감소 재적용 | all_allies | 솔린 쿨 추가 감소 |
| 27.50 | **솔린 burst_cast (2회째)** | 티켓(+1 → 캡 2라 **변화 없음**) · 안전하게 모실게! | all_allies | 티켓은 이미 포화 |
| 27.75 | full_burst 시작 (사이클 3) | 위 반복 | | |
| 이후 | 12.25s 사이클 · 솔린은 격 사이클 | | | |

사이클 간격 실측: 3.25 → 15.5 → 27.75 → 40.0 → 52.25 → 64.5 → 76.75 → … **12.25s 등간격**.
솔린 본인 버스트: t = 3.0 / 27.5 / 52.0 / 76.5 / 101.0 / … → **24.5s 간격**
(40s 쿨에서 사이클당 7.48s씩 두 번 깎여 실효 25.04s, 사이클 격자에 스냅되어 24.5s).

**티켓 게이지는 t=3.0에 2로 포화한 뒤 전투 끝까지 그대로다** (소모 경로 없음).

## 스킬 간 상호작용

- **`티켓` 게이지 → `티켓 효과` 값 스케일링**
  - 경로: 티켓(`gauge_charge`, `gauge_max: 2`) → 티켓 효과(`scaling: stack_count`,
    `scaling_ref: 티켓`) → `hp_only_caster_based_pct` = 솔린 base_hp × 10% × 게이지 수.
  - 시점: `passive` + `gauge_above:티켓:1`이라 **runtime 재판정**이다. 게이지가 바뀌면 값도 즉시 따라온다.
  - 위험: 이 경로는 **체력을 참조하는 아군이 있어야 딜이 된다.** 표준 스쿼드에는 없어 0 기여.
- **`버스트 스킬 재사용 시간 감소`가 buff인 것의 의미**
  - `burst_cooldown`(buff)은 `burst_cooldown_reduce`(instant)와 다른 stat이다. 이 캐릭터는
    **영구 buff**로 파싱돼 있는데, `BurstManager.tick()`의 `full_burst_start` 분기가
    **풀버스트 1회당 1회씩만** `burst_ready_at`을 당기고 `_cd_applied_at_cast`가 cast 시
    반영분과의 중복을 막는다(`docs/IMPL-STATUS.md`). → 정상 상태에서는 instant와 **동치**다.
  - 격리 대조로 확인: 솔린 단독 스쿼드 버스트 간격 **32.53s = 40 − 7.48**. 이중 적용 없음.
- **회복 체인 전체가 끊겨 있다**
  - `열차 탑승 도와줄게!`(`event:ally_hp_below:15`, notify 없음) → `열차 탑승 도와줄게! 2`
    (티켓 소모) → 티켓 감소 → 티켓 효과 약화. **첫 고리가 없어 체인 전체가 무발동**이다.
    그래서 티켓은 항상 캡 2다.

## 유저 확인 사항
- 초안 단계 없음. 아래는 파싱·코드·실측에서 확인한 사실.
- Q: 티켓은 최대 몇 장이고 언제 줄어드나?
  A: **2장 캡**, `battle_start`에 1 + 솔린 버스트마다 +1. 보스 DPS 시뮬에서는 **감소 경로가 없어**
  t=3.0 이후 항상 2다.
- Q: `burst_cooldown`(buff)으로 파싱된 게 이중 적용되지 않나?
  A: 되지 않는다. 격리 대조 실측 32.53s = 40 − 7.48.
- Q: `첫차 할인`이 파싱에 없는데 문제인가?
  A: 현재 구성에서는 **무해**하다. 회복 경로가 죽어 있어 면제권이 관여할 자리가 없다.
  회복·티켓 소모가 구현되면 그때 부여 항목을 추가해야 한다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 버스트 스킬 재사용 시간 감소 | 영구 `burst_cooldown` buff = 풀버스트당 1회 −7.48s | `BurstManager.tick()` `full_burst_start` 분기 · 실측 32.53s | `burst_cooldown_reduce` instant로 바꿔도 정상 상태 결과 동일. **다른 B1들은 전부 instant 쪽으로 파싱돼 있어 표기만 어긋나 있다** |
| 티켓 효과 | `hp_only_caster_based_pct` — **시전자(솔린) base_hp 기준**, 현재 체력은 유지 | 원문 `[시전자 기준 최대 체력만 {0}% X 티켓 수▲]` · `docs/PARSING.md` 매핑표 | `max_hp_only_pct`면 **수령자 자기 base_hp 기준**이 되어, 방어형·지원형이 앞열 딜러에게 주는 체력 버프가 과소평가된다. 지금 로스터에는 체력 참조 딜러가 없어 DPS는 불변 |
| 티켓 (게이지) | 캡 2, 보스전에서 감소 없음 | `gauge_max: 2` · 소모 트리거 무발동 · 실측 | 소모가 살아나면 회복 발생마다 티켓 효과가 절반으로 떨어진다 |
| 첫차 할인 해제 | 대상 없는 no-op | 부여 항목 미파싱 | 부여 항목이 생기면 초반 6초 티켓 보호가 실제로 동작 |

## impl/bug-fix용 검증 체크리스트
- [ ] `battle_start` 직후 티켓 게이지 = **1**, 솔린 첫 버스트 직후 = **2**, 이후 전투 끝까지 2 고정
- [ ] `티켓 효과`가 `hp_only_caster_based_pct` 10% × 게이지(=20%)로 재판정되는지 (`gauge_above` runtime)
- [ ] 풀버스트 진입마다 `버스트 스킬 재사용 시간 감소`가 아군 전체 쿨을 **1회씩만** 당기는지
- [ ] 솔린 단독 스쿼드 버스트 간격 **32.53s** (= 40 − 7.48, 이중 적용 없음)
- [ ] 표준 스쿼드 사이클 간격 12.25s / 솔린 본인 버스트 24.5s 간격 (격 사이클)
- [ ] 발동 금지: `열차 탑승 도와줄게!`·`열차 탑승 도와줄게! 2` — `event:ally_hp_below:15` 무발동 (0회)
- [ ] 발동 금지: 티켓 `gauge_consume` — 게이지가 2에서 내려가면 안 됨
- [ ] `안전하게 모실게!`·`heal_hp_pct`는 딜에 기여하지 않음 (회복 전용)

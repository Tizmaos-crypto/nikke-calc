# 소다 : 트윙클링 바니 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.
> **이 문서를 쓰면서 결함 2건을 고쳤다** — §고친 것.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (고 어헤드, 소다!)
- 무기 유형: SG (**9발**, **클립 3분할**, 재장전 0.67s, 계수 231.6%, 펠릿 10, 코어 200%, 연사 1.5/s)
- 속성 코드: 철갑 · 클래스: 화력형 · 기업: 테트라 · 스쿼드: 메이드 포 유
- **애장품 없음** · **기본 레이어 없음**
- 작성 사이클 수: **2** (쿨 40s)

> **이 캐릭터는 풀버스트 타임 자체를 5초 늘린다.** 스쿼드 전원의 풀버스트가
> 10초 → **15초**가 되고, 늘어난 구간에서 자기 `비기너즈 베네핏` 두 줄이
> 매 발 터진다. 대가로 **사이클이 길어져 180초 안의 풀버스트 횟수가 15회 → 11회**로
> 준다 — 조합에 따라 스쿼드 총딜이 오르기도 내리기도 한다(§양날).

## 효과 요약 (보강: 효과 단위 stat 키)

| # | ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 0 | 럭키 골든 칩 | 스킬1 | `battle_start` | `buff_stack_init`: **50** (→ `골든 칩`) | `self` | instant |
| 1 | 골든 칩 | 스킬1 | `on_attack_count:3` + `during_full_burst` | `crit_dmg`: 1.32% | `self` | 영구 · **50중첩** |
| 2 | 럭키 골든 칩 2 | 스킬1 | `on_attack_count:3` + `during_full_burst` | `atk_dmg_pct`: 10.51% | `["self", "allies_top_atk_excl:1"]` | 2s |
| 4 | 시간 연장 I | 스킬2 | `burst_enter:3` + `self_stack_above:골든 칩:10` | `fullburst_duration`: **+2s** | `all_allies` | — |
| 5 | 시간 연장 II | 스킬2 | `burst_enter:3` + `self_stack_above:골든 칩:20` | `fullburst_duration`: **+3s** | `all_allies` | — |
| 6 | 비기너즈 베네핏 I | 스킬2 | `on_attack` + `during_full_burst` + `self_state:시간 연장 I` | `damage`: 52.04% | `enemies_nearest:1` | — |
| 7 | 비기너즈 베네핏 II | 스킬2 | `on_attack` + `during_full_burst` + `self_state:시간 연장 II` | `damage`: 85.02% | `enemies_nearest:1` | — |
| 8 | 고 어헤드, 소다! | 스킬3 | `burst_cast` | `burst_damage`: 628.7% | `all_enemies` | — |
| 9 | 고 어헤드, 소다! 2 | 스킬3 | `burst_cast` + `self_stack_above:골든 칩:20` | `accuracy_pct`: 38.91% | `self` | 15s |
| 10 | 고 어헤드, 소다! 3 | 스킬3 | `burst_cast` + `self_stack_above:골든 칩:30` | `atk_pct`: 65.25% | `self` | 15s |
| 11 | 고 어헤드, 소다! 4 | 스킬3 | `burst_cast` | `buff_stack_remove`: **17** (→ `골든 칩`) | `self` | instant |

**미구현 stat은 없다.**
**배열 순서 제약**: `고 어헤드, 소다! 4`(중첩 17 소모)가 **맨 뒤**여야 한다 —
앞의 두 조건(`:20`·`:30`)이 소모 **전** 중첩으로 판정돼야 원문
`[효과 적용 후 골든 칩 중첩량 17개 ▼]`과 맞는다.

## 고친 것 (2026-09-03)

### ① `self_stack_above` 문턱이 전부 1씩 낮았다 (파싱)

원문은 `골든 칩 10 / 20 / 30 중첩 이상`인데 parsed가 `:9 / :19 / :29`였다.
`self_stack_above:X:N`은 `현재 >= N`이므로(`buff_manager.py` 두 곳 모두
`if current < threshold: return False`) **한 중첩 일찍 열린다**.
`PARSING.md §N 중첩 이상`도 N을 그대로 쓰라고 하고, **전 캐릭터 스캔 결과
이 유형은 소다 : 트윙클링 바니 하나뿐이었다**(나유타 2/10/30 · 퀀시 10/10/5 ·
그 밖 8명은 전부 최대 중첩 값 그대로).

표준 스쿼드에서 실제로 물렸다. 버스트 시점 중첩이 50 → 44 → 39 → 34 → **29** →
24 → **19**로 내려가는데, 5번째(29)에서 `고 어헤드 3`이, 7번째(19)에서
`고 어헤드 2`가 원문대로면 안 열려야 하는데 열리고 있었다.
개인 딜 489,539,635 → **471,907,648 (−3.60%)**.

### ② 같은 캐릭터의 `fullburst_duration` 두 줄이 하나로 뭉개졌다 (미구현/버그)

`BurstController`의 합산 루프가 중복 제거 키를 **caster 하나**로 잡고 있었다.
`all_allies` 대상이라 같은 효과가 N명에게 등록되는 걸 막으려던 것인데,
**한 캐릭터가 가진 서로 다른 두 효과**까지 접어 버렸다. 소다는
`시간 연장 I`(+2)과 `시간 연장 II`(+3)를 `[하위 효과 중복 적용]`으로 함께 갖는
**유일한 캐릭터**라 이 자리에서만 드러난다.

실측 풀버스트 길이 **12.0초 → 15.0초**(10 + 2 + 3). 키를
`(caster, source, name)`으로 바꿨다. `fullburst_duration` 보유자 넷 중
둘 이상을 가진 캐릭터가 소다뿐이라 다른 셋(이사벨 −5 · 모더니아 +5)은 무영향이다.

두 수정의 합: `S39_소다` baseline **스쿼드 +8.48% · 소다 +11.71%**
(하네스 33개 중 이 하나만 움직인다).

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "소다 : 트윙클링 바니", "test_B3"]`
  - 적 코드는 **철갑**(자기 코드 = 우월 코드 불성립)이 기준이다.
- **변형 1 (우월 코드)**: 표준 + `enemy={"code": "전격"}` — 개인 딜 **×2.18**.
  캐릭터 고유 `element_bonus_pct`가 없어 공통 장비·큐브 107.69%만 실린다.
- **변형 2 (코어 보스)**: 표준 + `enemy={"core_px": 52}` — 개인 딜 **+1.7%**뿐.
  SG 탄착군 250px에 명중률이 `고 어헤드 2`(38.91, 자기 버스트 15초)밖에 없어
  코어 확률이 낮다. **도로시 : 세렌디피티(+34.4%)와 정반대**다.

- **실전 조합 (하네스)** — `S39_소다` **1개**뿐이다:

  | 스쿼드 | 적 | 스쿼드 총딜 | 소다 : 트윙클링 바니 | 풀버스트 |
  |---|---|---|---|---|
  | `S39_소다` | 전격 (**우월 코드**) | 4,127,828,614 | **2,139,404,631 (51.8%)** | 9회 |

  `시간 연장 I·II`가 각각 **45건**(= 9 풀버스트 × 5명)이다.
  `고 어헤드 2·3`은 소다의 버스트 5회 전부에서 열린다 — 이 조합은
  풀버스트가 길어 중첩 회복이 소모(17)를 따라잡는다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층):

| 설정 | 스쿼드 총딜 | 소다 : 트윙클링 바니 | 풀버스트 |
|---|---|---|---|
| 표준 (적 철갑) | 1,775,997,109 | **487,354,742 (27.4%)** | **11회** |
| 표준 · 적 **전격**(우월 코드) | 2,122,017,703 | 1,060,922,655 (50.0%) | 11회 |
| 표준 · 코어 52px | 2,029,595,397 | 495,675,042 (24.4%) | 11회 |

딜 구성 (표준 · 적 철갑):

| 출처 | 딜 | 비중 | 히트 | 히트당 |
|---|---|---|---|---|
| 기본 공격 (펠릿) | 315,997,030 | 64.8% | 2,620 | 120,609 |
| `비기너즈 베네핏 II` | 101,337,645 | 20.8% | 222 | 456,475 |
| `비기너즈 베네핏 I` | 62,027,866 | 12.7% | 222 | 279,404 |
| `고 어헤드, 소다!` (버스트) | 7,992,201 | 1.6% | 6 | 1,332,033 |

버스트 구간별: **풀버스트(본인) 65.4%** · 풀버스트(타인) 29.5% · 비풀버스트 5.1%.

## 풀버스트 5초 연장은 양날이다

`시간 연장 I·II`는 **`burst_enter:3`**(3단계 자리가 열리는 순간)에 붙는다 —
소다가 그 자리를 쓰든 안 쓰든 **매 사이클** 판정된다. 표준 실측 발동
**11건 × 4명**으로 풀버스트 11회 전부에 걸린다.

| | 연장 없음 | `시간 연장 I`만 (+2) | **I + II (+5)** |
|---|---|---|---|
| 풀버스트 길이 | 10.0s | 12.0s | **15.0s** |
| 180초 풀버스트 횟수 | 15 | 14 | **11** |
| 사이클 간격 | 12.533s | 14.25s | **17.25s** |

**풀버스트가 길어질수록 사이클 수가 준다.** 그래서 스쿼드 총딜의 방향이
조합마다 다르다 — 실측:

| 스쿼드 | 결함 ② 수정 전 (풀버스트 12s) | 수정 후 (15s) | 방향 |
|---|---|---|---|
| 표준 4인 | 스쿼드 1,859,370,332 / 소다 471,907,648 | 1,775,997,109 / **487,354,742** | 스쿼드 **−4.5%** · 소다 **+3.3%** |
| `S39_소다` | 스쿼드 3,805,034,966 / 소다 1,915,193,354 | 4,127,828,614 / 2,139,404,631 | 스쿼드 **+8.5%** · 소다 **+11.7%** |

표준 스쿼드에서는 리틀 머메이드가 사이클 수에 강하게 묶여 있어 손해가 나고
(1,094.6M → 1,015.5M), `S39_소다`에서는 전원이 풀버스트 안에서 딜을 내는 구성이라
이득이 난다. **소다 본인은 두 경우 모두 이득**이다 — 연장 구간이 곧
`비기너즈 베네핏`의 발동 창이기 때문이다.

> 아르카나 : 포츈 메이트의 6발 사이클(`PARSING-CHARS`)이 소다의 연장으로 사이클당
> 26발까지 늘어난다는 기존 기록도 이 연장을 전제로 한 것이다.
> 이사벨 `소닉 체이서 5`(−5초)가 **부호만 반대인 같은 stat**이다.

## `비기너즈 베네핏` — 풀버스트 중 매 발 두 방

`on_attack` + `during_full_burst` + `self_state:시간 연장 I / II`.
두 상태가 다 서 있으면 **한 발에 두 효과가 같이** 난다(원문 `[하위 효과 중복 적용]`).

표준 실측 **222회씩** — 풀버스트 11회 × 15초 동안 연사 1.5/s로 쏜 발수와 같다
(재장전으로 빠지는 만큼이 차이다). 합계 163,365,511로 **개인 딜의 33.5%**다.

이 둘은 `기본 공격`과 달리 **펠릿으로 쪼개지지 않는다**(`stat: damage`,
`enemies_nearest:1`). 히트당 279,404 / 456,475로 펠릿 하나(120,609)보다 크다.

## 골든 칩 — 매 버스트 17개를 쓰고 풀버스트에서 채운다

| 이벤트 | 중첩 변화 |
|---|---|
| `battle_start` (`럭키 골든 칩`) | **50** (= max_stack) |
| 자기 `burst_cast` (`고 어헤드, 소다! 4`) | **−17** |
| 풀버스트 중 3발마다 (`골든 칩`) | **+1** (상한 50) |

표준(수정 후) 버스트 시점 중첩 실측: **50 → 48 → 46 → 43 → 40 → 37** (6회).
풀버스트가 15초로 길어 회복(+15 안팎)이 소모(17)를 거의 따라잡는다 —
그래서 `고 어헤드 2`(≥20)·`3`(≥30)이 **6회 전부** 열린다.

> 결함 ② 수정 **전**(풀버스트 12초)에는 회복이 +11밖에 안 돼
> **50 → 44 → 39 → 34 → 29 → 24 → 19**로 내려갔다. 그 상태에서 결함 ①까지 겹쳐
> 29에서 `고 어헤드 3`이, 19에서 `고 어헤드 2`가 잘못 열렸다. **두 결함이 같은
> 자리에서 만난다** — 연장이 짧아 중첩이 마르고, 문턱이 낮아 마른 걸 못 잡았다.

`골든 칩`의 본체 효과는 `crit_dmg` 1.32%/중첩이다. 50중첩이면 **크리티컬 대미지 +66%**
이고, 이건 중첩이 있는 한 항상 실린다. 실측 발동 92회(= 풀버스트 중 3발마다).

`럭키 골든 칩 2`는 같은 트리거로 **자신 + 자신 제외 최종 공격력 1위 아군**에게
공격 대미지 10.51%를 2초 준다. 표준 실측 자신 74 · 리틀 머메이드 38 · test_B3 36 —
**아군 쪽 수령자가 도중에 바뀐다**(`allies_top_atk_excl:1`은 매 발동 시점의 1위다).

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 소다 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 비고 |
|---|---|---|---|
| 0.000 | 전투 시작 | **럭키 골든 칩** — `골든 칩` **50중첩** | 시작부터 최대 중첩 |
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | | |
| **3.100** | **`burst_enter:3`** | **시간 연장 I(+2) · II(+3)** → **아군 4명 전원** | **소모 전 중첩(50)으로 판정** |
| **3.200** | **소다 burst_cast** | **고 어헤드 2**(명중률 +38.91) · **3**(공격력 +65.25) · **고 어헤드!** 버스트 딜 1,242,914 · **고 어헤드 4**(중첩 −17 → 33) | 조건은 50중첩으로 이미 통과 |
| 3.250 | full_burst 시작 | | **길이 15.0초** (10 + 2 + 3) |
| 3.25~18.25 | 발사마다 | **비기너즈 베네핏 I·II** (매 발 2히트) · 3발마다 **골든 칩** +1 · **럭키 골든 칩 2** | 연장된 5초가 그대로 딜 구간 |
| 17.333 → 17.883 | **클립 재장전 ×3** | | 0.55초 (클립 3분할: 17.517 · 17.700 · 17.883) |
| 18.200 | 고 어헤드 2·3 만료 | | 15초 |
| **18.250** | **full_burst 종료** | | |

### 사이클 2 — 소다 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 20.250 · 20.350 | 리틀 머메이드 · 크라운 | | |
| **20.350** | `burst_enter:3` | **시간 연장 I·II** (다시 4명 전원) | **소다가 안 눌러도 걸린다** |
| 20.450 | test_B3 stage:3 | | 소다는 쿨 |
| 20.500 | full_burst 시작 | | 역시 15.0초 |
| 20.5~35.5 | 발사마다 | 비기너즈 베네핏 I·II · 골든 칩 | **자기 버스트와 무관하게 계속 난다** |
| **37.700** | 소다 burst_cast (2회째) | 중첩 48 → 31 | **간격 34.5s** |

사이클 간격 **17.25s**(180초 11회), 소다 버스트 **6회**.

## 스킬 간 상호작용

- **`시간 연장` → `비기너즈 베네핏`** *(연장이 곧 발동 창)*
  - `during_full_burst` 게이트라 풀버스트가 10 → 15초가 되면 발동 창이 1.5배가 된다.
    소다 본인에게는 순이득이지만 사이클 수가 줄어 스쿼드에는 방향이 갈린다.
- **`시간 연장` ↔ 사이클 수** *(양날)*
  - 풀버스트가 길수록 180초 안의 사이클이 준다(15 → 11회). 풀버스트 밖 딜 비중이
    큰 아군(표준의 리틀 머메이드)은 손해를 본다.
- **`골든 칩` 중첩 ↔ 자기 버스트** *(자기 소모 · 자기 회복)*
  - 버스트마다 17을 쓰고 풀버스트 중 3발마다 1을 채운다. 회복량이 곧 풀버스트
    길이에 비례하므로 **`시간 연장`이 중첩 수지를 스스로 지탱한다**.
  - 중첩이 마르면 `고 어헤드 2·3`(명중률·공격력)이 닫히고, 더 내려가면
    `시간 연장` 자체가 닫혀 **연장 → 회복 → 연장의 고리가 끊긴다**.
- **`고 어헤드 2`(명중률) ↔ 코어** *(약하다)*
  - SG 탄착군 250px을 38.91%로 줄여도 152.7px이라 코어 확률이 낮다.
    코어 보스에서 개인 딜이 +1.7%뿐인 이유다.
- **`럭키 골든 칩 2` → 아군 1위** *(수령자가 바뀐다)*
  - `allies_top_atk_excl:1`은 발동 시점의 최종 공격력 1위를 고른다.
    표준에서 리틀 머메이드 38회 · test_B3 36회로 갈렸다.

## 유저 확인 사항
- Q: `골든 칩` 문턱을 10/20/30으로 되돌린 게 맞나?
  A: **원문 그대로다.** `self_stack_above:X:N`은 `현재 >= N`이고 `PARSING.md`도 N을
  그대로 쓰라고 한다. 나머지 10명은 전부 원문 값이었다 — 이 캐릭터만 1씩 낮았다.
- Q: 풀버스트 5초 연장이 스쿼드 총딜을 **떨어뜨리는** 게 정상인가?
  A: **조합에 달렸다.** 연장은 사이클 수를 깎는다(15 → 11회). 표준 4인에서는
  스쿼드 −4.5%, `S39_소다`에서는 +8.5%다. 소다 본인은 둘 다 이득이다.
- Q: `시간 연장`이 왜 `[풀 버스트 타임 동안 지속]`인데 영구 버프로 등록되나?
  A: **`fullburst_duration`은 게임 내 instant인데 진입 시점에 값을 읽어야 해서
  buff로 보관하는 구조다**(`IMPL-STATUS §fullburst_duration`). 다만 소다는 이 버프를
  `self_state:시간 연장 I/II`로도 쓰므로, **한 번 걸린 뒤 중첩이 떨어져도 상태 판정은
  계속 참**이 된다. 표준·하네스 모두 매 사이클 재발동해 실측에는 안 드러나지만,
  중첩이 마른 조합에서는 `비기너즈 베네핏`이 과대평가될 수 있다. (미해결 · 기록만)
- Q: 코어 보스에서 왜 거의 안 오르나?
  A: **SG 탄착군 250px에 명중률이 자기 버스트 15초의 38.91%뿐**이기 때문이다.
  같은 SG인 도로시 : 세렌디피티는 `섬광` 창에서 138.86%가 붙어 탄착군이 1px까지
  줄지만, 소다는 그런 창이 없다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `self_stack_above:골든 칩:N` | **N 이상**, N은 원문 그대로 10/20/30 | 원문 `N 중첩 이상` · `buff_manager` `current < threshold` · `PARSING §353` | 1 낮으면 중첩이 마른 사이클에서 잘못 열린다(실측 2건) |
| 고 어헤드, 소다! 4 | 배열 **맨 뒤** — 조건 판정 후 소모 | 원문 `[효과 적용 후 골든 칩 중첩량 17개 ▼]` | 앞이었다면 33중첩으로 판정돼 `고 어헤드 3`이 대부분 닫힌다 |
| 시간 연장 I·II | **둘 다 더한다**(+5) — `[하위 효과 중복 적용]` | 원문 · 실측 풀버스트 15.0초 | 하나만 세면 12.0초 (수정 전 상태) |
| 시간 연장 | `burst_enter:3` — **소다가 안 눌러도** 매 사이클 | 원문 `버스트 3단계 진입 시` · 실측 11회 전부 | `burst_cast`였다면 격 사이클로 반토막 난다 |
| 시간 연장 | 대상 `all_allies`지만 지속 시간 기여는 **효과당 1회** | `BurstController` 중복 제거 키 `(caster, source, name)` | caster 키였다면 두 줄이 하나로 뭉개진다(수정 전 결함) |
| 비기너즈 베네핏 I·II | `on_attack` + `during_full_burst` + `self_state` — **펠릿 분할 없음** | 원문 `조준선에 가장 가까운 적 1기에게` · `enemies_nearest:1` | 펠릿 분할이었다면 히트당 1/10이 된다 |
| 럭키 골든 칩 | `buff_stack_init` 50 — **전투 시작에 최대 중첩** | 원문 `[골든 칩 중첩량 50개 ▲]` (lv10) | 0에서 쌓았다면 첫 버스트에 아무 조건도 안 열린다 |
| 럭키 골든 칩 2 | 자신 + `allies_top_atk_excl:1` **복합 대상 1항목**(`target` 배열) | 원문 `자신과 자신을 제외한 … 아군 1기에게` · `PARSING.md` §5 | `allies_top_atk:1` 하나였다면 자신이 1위일 때 아군이 못 받는다. 2026-09-05 이전에는 같은 이름의 두 항목이라 Step 7 위반이었다 — 병합 후 L2 발동 172회·수령자 집합 동일 |

## impl/bug-fix용 검증 체크리스트
- [ ] 이탈 보고가 **`기본 스펙(1층) 그대로`** 한 줄 (레이어·애장품 없음)
- [ ] 풀버스트 길이 **15.0초** (3.250 → 18.250) — `시간 연장 I + II` 합산
- [ ] 180초 풀버스트 **11회** · 사이클 간격 **17.25s** · 소다 버스트 **6회**
- [ ] `시간 연장 I`·`II` 각각 **11회 × 4명** (`burst_enter:3`, 소다 미사용 사이클 포함)
- [ ] `시간 연장`이 소다의 **버스트 전(3.100)** 에 판정된다 — 소모 전 중첩 50
- [ ] 버스트 시점 중첩 **50 → 48 → 46 → 43 → 40 → 37** (회복이 소모 17을 거의 상쇄)
- [ ] `고 어헤드 2`·`3` 각각 **6회** (버스트 6회 전부, 중첩이 20·30 아래로 안 내려간다)
- [ ] `비기너즈 베네핏 I`·`II` 각각 **222회**, 합쳐 개인 딜의 **33.5%**
- [ ] `골든 칩` **92회** (풀버스트 중 3발마다) · `럭키 골든 칩 2` 자신 74 / 아군 38+36
- [ ] 코어 52px에서 개인 딜 **+1.7%**뿐 (네거티브 — SG 탄착군이 안 줄어든다)
- [ ] 적 전격(우월 코드)에서 개인 딜 **×2.18** — 캐릭터 고유 `element_bonus_pct` 없음
- [ ] `S39_소다` baseline 스쿼드 **+8.48%** · 소다 **+11.71%** (하네스 33개 중 이것만 움직인다)

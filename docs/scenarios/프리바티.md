# 프리바티 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (AK 미사일)
- 무기 유형: AR (**60발**, 재장전 1.0s, 계수 13.65%, 코어 200%, 연사 12/s)
- 속성 코드: 수냉 · 클래스: 화력형 · 기업: 엘리시온 · 스쿼드: 트라이앵글
- **애장품: 「중앙 정부 특별 훈장」 — 슬롯 순서 [3, 2, 1]** (기본 스펙은 **3단계**)
  → 1단계가 **스킬3**, 2단계가 **스킬2**, 3단계가 **스킬1**을 바꾼다
- 작성 사이클 수: **2** (쿨 40s)
- **기본 레이어 1건** (`data/char_defaults.json`): 최대 장탄 옵션 **0%** — **이 캐릭터에서는 결정적이다**

> **탄창을 빨리 비우는 것이 이 캐릭터의 딜이다.** 딜의 63%가 `last_bullet`(마지막 탄환 명중)에
> 달려 있고, 본인의 `EX 매거진 3`이 **아군 전체의 최대 장탄을 50.66% 깎아** 그 주기를 스스로
> 당긴다. 그래서 장탄 옵션 0% 레이어를 벗기면 개인 딜이 **−43.2%**가 된다.

## 효과 요약 (보강: 효과 단위 stat 키)

애장품 슬롯마다 판본이 둘이다(기본 / 애장품). 기본 스펙은 3단계라 **애장품 판본이 도는 것**이다.

| # | ID (parsed name) | 스킬 | 판본 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|---|
| 0 | EX 매거진 | 스킬1 | 기본 | `full_burst_start` | `atk_pct`: 23.61% | `all_allies` | 10s |
| 1 | EX 매거진 2 | 스킬1 | 기본 | `full_burst_start` | `reload_speed_pct`: 51.16% | `all_allies` | 10s |
| 2 | EX 매거진 3 | 스킬1 | 기본 | `full_burst_start` | `max_ammo_pct`: **−50.66%** | `all_allies` | 10s |
| 3~5 | EX 매거진 1·2·3 | 스킬1 | **애장품3** | 〃 | 값 동일 | 〃 | 〃 |
| 6 | EX 매거진 4 | 스킬1 | **애장품3** | `full_burst_start` | `atk_dmg_pct`: 20.16% | `all_allies` | 10s |
| 7 | LD 어설트 2 | 스킬2 | 기본 | `last_bullet` | `bonus_damage`: 85.79% | `target` | — |
| 8 | LD 어설트 3 | 스킬2 | 기본 | `last_bullet` + **`target_stunned`** | `bonus_damage`: 1089% | `same_target` | — |
| 9 | LD 어설트 | 스킬2 | **애장품2** | `last_bullet` | `received_dmg_pct`: 10.01% | `target` | 10s |
| 10 | LD 어설트 2 | 스킬2 | **애장품2** | `last_bullet` | `bonus_damage`: **256.17%** | `target` | — |
| 11 | LD 어설트 3 | 스킬2 | **애장품2** | `last_bullet` + **`target_state:타겟 지정`** | `bonus_damage`: **1687%** | `same_target` | — |
| 12 | AK 미사일 2 | 스킬3 | 기본 | `burst_cast` | `burst_damage`: 457.87% | `all_enemies` | — |
| 13 | AK 미사일 3 | 스킬3 | 기본 | `burst_cast` | `stun` | `all_enemies` | 3s |
| 14 | AK 미사일 | 스킬3 | **애장품1** | `burst_cast` | `element_bonus_pct`: **130%** | `self` | 10s |
| 15 | AK 미사일 2 | 스킬3 | **애장품1** | `burst_cast` | `burst_damage`: **1407.64%** | `all_enemies` | — |
| 16 | AK 미사일 3 | 스킬3 | **애장품1** | `burst_cast` | `stun` | `all_enemies` | 3s |
| 17 | 타겟 지정 | 스킬3 | **애장품1** | `burst_cast` | `atk_pct`: **−5.02%** | `all_enemies` | 10s |

**미구현 stat은 없다.**

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "프리바티", "test_B3"]`
  - 적 코드는 **수냉**(자기 코드 = 우월 코드 불성립)이 기준이다.
- **변형 1 (작열 적 — 필수)**: 표준 + `enemy={"code": "작열"}`
  - `AK 미사일`(`element_bonus_pct` **130%**)은 **우월 코드가 성립할 때만 산다.**
    실측 481,298,979 → **1,521,851,260 (×3.16)**.
  - **하네스 5개 스쿼드 중 프리바티가 우월 코드를 받는 자리는 하나도 없다** —
    적 코드가 수냉·수냉·철갑·전격·풍압이다. 130%짜리 버프가 실전 회귀에서 통째로 죽어 있다.
- **변형 2 (장탄 레이어 이탈 — 필수)**: 표준 + `chars={"프리바티": {"equip_skills": {"max_ammo_pct": 129.64}}}`
  - `last_bullet` 주기가 늘어 `LD 어설트`가 56회 → **14회**로 준다. 개인 딜 **−43.2%**.
- **변형 3 (애장품 단계)**: `--favorite "프리바티:N"` — 0/1/2 각각. 2단계가 계단이다(아래).

- **실전 조합 (하네스)** — 프리바티는 **5개 스쿼드**에 들어 있다. 로스터에서 가장 많이 쓰이는 자리다:

  | 스쿼드 | 적 | 스쿼드 총딜 | 프리바티 | 버스트 |
  |---|---|---|---|---|
  | `S39_나가라피` | 전격 | 5,940,710,092 | 655,416,530 (11.0%) | **씀** |
  | `S38_마나` | 철갑 | 6,336,518,822 | 414,830,390 (6.5%) | **씀** |
  | `S36_서머메이든` | 수냉 | 6,322,888,418 | 429,737,817 (6.8%) | **씀** (6회) |
  | `S40_브리드라피` | 풍압 | 8,245,774,042 | 294,733,687 (3.6%) | 안 씀 |
  | `S36_트리나홍련` | 수냉 | 5,191,085,100 | 204,462,230 (3.9%) | 안 씀 |

  **버스트를 쓰는 세 자리에서만 `LD 어설트 3`과 `AK 미사일 2`가 나온다.**
  안 쓰는 두 자리에서는 개인 딜이 `기본 공격 + LD 어설트 2`뿐이고, 프리바티는
  사실상 **`EX 매거진` 전용 버퍼**로 도는 것이다.

  > `S36_서머메이든`은 **2026-09-03부터** 버스트를 쓴다. 메이든 : 아이스 로즈에게
  > 버스트 패턴 `[12]`를 걸어 MP 상한을 열었더니(그쪽 시나리오 §MP), 비게 된 B3 자리를
  > 프리바티가 가져갔다. 개인 딜 221,918,354 → **429,737,817 (+93.7%)**이고
  > `LD 어설트 3` 18히트·`AK 미사일 2` 6히트가 새로 잡힌다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층 + 장탄 레이어, 애장품 3):

| 설정 | 스쿼드 총딜 | 프리바티 |
|---|---|---|
| 표준 (적 수냉) | 1,626,048,646 | **481,298,979 (29.6%)** |
| 표준 · 적 **작열**(우월 코드) | 2,666,600,927 | 1,521,851,260 (57.1%) |
| 표준 · 장탄 옵션 129.64% (레이어 이탈) | 1,390,382,902 | 273,256,022 (19.7%) |
| 애장품 2 | 1,459,331,493 | 432,503,297 |
| 애장품 1 | 1,140,964,277 | 206,481,499 |
| 애장품 0 | 1,140,002,684 | 194,044,190 |

딜 구성 (표준 · 적 수냉):

| 출처 | 딜 | 비중 | 히트 |
|---|---|---|---|
| `LD 어설트 3` (타겟 지정 창) | 229,449,570 | **47.7%** | 21 (히트당 10.4M) |
| 기본 공격 | 150,502,018 | 31.3% | 2,104 |
| `LD 어설트 2` | 75,817,620 | 15.7% | 56 |
| `AK 미사일 2` (버스트) | 25,529,771 | 5.3% | 8 |

버스트 구간별: **풀버스트(본인) 71.2%** · 풀버스트(타인) 19.0% · 비풀버스트 9.9%.

## 스스로 탄창을 깎아 `last_bullet`을 당긴다

`EX 매거진 3`(`max_ammo_pct −50.66`)은 **아군 전체**에게 걸리고, 거기에 자신도 포함된다.
같은 시점에 `EX 매거진 2`가 재장전 속도 +51.16%를 준다. 둘이 겹치는 풀버스트 10초 동안
프리바티의 탄창 순환이 절반으로 짧아진다:

| 구간 | 탄창 | `last_bullet` 간격 (실측) |
|---|---|---|
| 풀버스트 안 | 60 × (1 − 0.5066) ≈ **30발** | **≈ 2.5초** (5.683 · 8.200 · 10.717 · 13.233) |
| 풀버스트 밖 | 60발 | **≈ 5.0초** (13.233 → 18.250) |

재장전도 실측 **0.017초**(1.0s에 재장전 속도 버프)로 사실상 끊김이 없다.

`EX 매거진`은 `full_burst_start`라 **누가 버스트했든 매 풀버스트에 걸린다**(180초 15회 ×
4명 = 60건). 그래서 프리바티가 버스트를 안 쓰는 조합에서도 이 가속만은 그대로 돈다 —
위 하네스 표에서 버스트를 안 쓰는 두 자리도 `LD 어설트 2`가 **56~57회**로 같은 이유다.

## `타겟 지정` 10초 창이 딜의 절반이다

`LD 어설트 3`(1687%)의 게이트는 애장품 단계에 따라 **완전히 다른 조건**으로 바뀐다:

| 애장품 | `LD 어설트 3` 조건 | 창 길이 | 계수 | 실측 발동 |
|---|---|---|---|---|
| 0·1 | `target_stunned` (`AK 미사일 3`의 기절) | **3초** | 1089% | **7회** |
| 2·3 | `target_state:타겟 지정` | **10초** | 1687% | **21회** |

기절 창(3초)에는 `last_bullet`이 한 번 들어갈까 말까이고, `타겟 지정` 창(10초)에는
**풀버스트 가속이 겹쳐 2~3회**가 들어간다. 계수 1.55배 × 발동 3배 = 이 효과 딜이
39,089,981 → **229,449,570 (5.9배)**가 된다.

두 조건 모두 **자기 버스트가 만든다**(`AK 미사일 3` 기절 / `타겟 지정`). 그래서
**프리바티가 버스트를 못 쓰는 사이클에는 `LD 어설트 3`이 아예 없다** — 하네스
`S40_브리드라피`·`S36_트리나홍련` 둘이 그 상태다.

> `LD 어설트 3`의 기본 판본 조건은 `target_stunned`다 — **이름 있는 상태가 아니라 기절 자체**를
> 보므로 다른 캐릭터가 건 기절도 조건을 만족한다(parsed `note`). 애장품 2단계 판본은
> `타겟 지정`이라는 **자기 이름 상태**로 바뀌므로 그 성질을 잃는다.

## 애장품 계단은 2단계에 몰려 있다

슬롯 순서가 `[3, 2, 1]`이라 **1단계가 스킬3, 2단계가 스킬2, 3단계가 스킬1**을 바꾼다.

| 단계 | 바뀌는 것 | 프리바티 | 스쿼드 총딜 |
|---|---|---|---|
| 0 | — | 194,044,190 | 1,140,002,684 |
| 1 | `AK 미사일 2` 457.87 → **1407.64** · `AK 미사일`(우월 코드 130%) · `타겟 지정` 신설 | 206,481,499 (+6.4%) | 1,140,964,277 (+0.1%) |
| 2 | `LD 어설트 2` 85.79 → **256.17** · `LD 어설트 3` 조건이 **기절 → 타겟 지정** · `LD 어설트`(받는 대미지 10.01) 신설 | 432,503,297 (**+109.5%**) | 1,459,331,493 (+27.9%) |
| 3 | `EX 매거진 4`(공격 대미지 20.16, 아군 전체) 신설 | 481,298,979 (+11.3%) | 1,626,048,646 (+11.4%) |

**1단계는 표준 스쿼드에서 거의 무의미하다** — 버스트 계수 3배는 총딜의 5%짜리 항목이고,
`AK 미사일`의 우월 코드 130%는 적 코드가 작열이 아니면 **0**이기 때문이다.
2단계가 진짜 계단이고, 3단계는 본인이 아니라 **스쿼드 전체**를 올린다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 프리바티 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | — | | |
| **3.200** | **프리바티 burst_cast** | **AK 미사일**(우월 코드 +130) · **AK 미사일 3**(기절) · **타겟 지정**(적 공격력 −5.02) · **AK 미사일 2** 2,782,847 | self 10s / 적 3s / 적 10s / 적 | 우월 코드 버프는 적 코드가 작열이 아니면 **무효** |
| **3.250** | full_burst 시작 (1회째) | **EX 매거진 1~4** (공격력 +23.61 · 재장전 +51.16 · **최대 장탄 −50.66** · 공격 대미지 +20.16) | 아군 전체 · 10s | 여기서 탄창이 반으로 준다 |
| **5.683** | **마지막 탄환 명중** | **LD 어설트**(받는 대미지 +10.01) · **LD 어설트 2** 1,575,997 · **LD 어설트 3** 10,378,679 | 적 | `타겟 지정` 창 안이라 3번이 열린다 |
| 5.767 → 5.783 | 재장전 | | | **0.017초** — `EX 매거진 2` 효과 |
| **6.217** | 기절 만료 | AK 미사일 3 off | | 3초짜리라 두 번째 `last_bullet`에 못 닿는다 |
| 8.200 · 10.717 | 마지막 탄환 | LD 어설트 2·3 (동일값) | | **2.5초 주기** |
| 13.233 | 마지막 탄환 | LD 어설트 2만 | | `타겟 지정`이 13.200에 끝났다 |
| 13.250 | full_burst 종료 | EX 매거진 1~4 off | | 탄창 60발로 복귀 |

### 사이클 2 — 프리바티 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | — | 프리바티는 쿨 |
| **15.783** | full_burst 시작 (2회째) | **EX 매거진 1~4** | **프리바티가 안 썼는데도 걸린다** (`full_burst_start`) |
| 18.250 · 20.783 · 23.317 | 마지막 탄환 | **LD 어설트 2만** (1,326,568) | `타겟 지정`이 없어 3번은 안 열린다 |
| 25.783 | full_burst 종료 | | |
| 25.850 | 마지막 탄환 | LD 어설트 2 (518,574) | 버프가 빠져 딜이 1/2.6 |
| **28.267** | 프리바티 burst_cast (2회째) | 사이클 1과 동일 | **간격 25.067s** |

사이클 간격 **12.533s 등간격**(180초 15회), 프리바티 버스트 8회.

## 스킬 간 상호작용

- **`EX 매거진 3` → 자기 `last_bullet`** *(순환 강화)*
  - 경로: 자기 버프가 자기 탄창을 깎아 → `last_bullet` 주기가 절반 → `LD 어설트` 3종이 두 배로 난다.
  - 시점: `full_burst_start`라 **자기 버스트 여부와 무관**하게 매 풀버스트에 걸린다.
  - 위험: 장탄 옵션이 붙으면 이 순환의 이득이 상쇄된다 — 레이어 0%가 그래서 있다.
- **자기 버스트 → `LD 어설트 3` 게이트** *(게이지가 아니라 상태)*
  - `타겟 지정`(애장품2 이상) 또는 `기절`(그 이하)이 열려 있어야 1687%가 난다.
  - 창이 10초, 풀버스트도 10초라 **거의 정확히 겹친다**. 두 창이 어긋나는 조합
    (버스트 딜레이·재진입)에서는 발동 수가 줄 수 있다.
- **`AK 미사일`(우월 코드 130%) ↔ 적 코드** *(적 의존 · 실전에서 죽어 있다)*
  - `element_bonus_pct`는 우월 코드가 성립할 때만 대미지에 실린다. 프리바티는 수냉이므로
    **작열 적**에서만 산다. 하네스 5개 스쿼드에 그런 자리가 없다.
  - 실측: 표준 481.3M → 작열 적 1,521.9M(×3.16). 이 차이의 대부분이 이 버프다.
- **`EX 매거진`은 아군 전체 버프다** *(프리바티가 5개 스쿼드에 있는 이유)*
  - 공격력 23.61 + 재장전 51.16 + 공격 대미지 20.16(애장품3)을 **매 풀버스트** 전원에게.
    최대 장탄 −50.66%는 탄창이 큰 아군에게는 손해가 될 수 있지만, 재장전 속도와 짝이라
    대체로 회전이 빨라진다.
- **`타겟 지정`의 `atk_pct −5.02`는 적 공격력 감소다** *(딜과 무관)*
  - 적에게 거는 효과라 **딜 계산에는 영향이 0**이다. 게이트 역할만 한다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: 장탄 옵션 0% 레이어가 왜 이 캐릭터에서 그렇게 큰가?
  A: **딜의 63%가 `last_bullet` 트리거이기 때문이다.** 탄창이 커지면 그 주기가 늘어
  `LD 어설트`가 56회 → 14회로 준다. 개인 딜 −43.2%, 스쿼드 −14.5%.
- Q: `LD 어설트 3`이 왜 21회뿐인가? `last_bullet`은 56회인데.
  A: **`타겟 지정` 창(자기 버스트 후 10초) 안에서만 열린다.** 8번 버스트 × 10초 = 80초 구간에
  들어간 `last_bullet`이 21회다.
- Q: 애장품 1단계가 왜 거의 이득이 없나?
  A: **1단계가 올리는 두 축이 표준 설정에서 둘 다 작다.** 버스트 계수는 총딜의 5%짜리이고,
  우월 코드 130%는 적 코드가 작열이 아니면 0이다.
- Q: 프리바티가 버스트를 안 쓰는 하네스 스쿼드는 파싱이 잘못된 건가?
  A: **아니다.** 같은 B3 자리에 다른 캐릭터가 있어서다. 그 조합에서는 `LD 어설트 3`·
  `AK 미사일 2`가 없고 `EX 매거진` 버퍼로만 돈다 — 하네스 두 스쿼드가 그 상태다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| EX 매거진 3 | `max_ammo_pct` **−50.66 고정값**, 대상 `all_allies`(자신 포함) | 원문 `[최대 장탄 수 50.66% ▼]` · 실측 풀버스트 중 `last_bullet` 주기 절반 | 자신 제외였다면 이 캐릭터의 순환 강화가 통째로 사라진다 |
| EX 매거진 계열 | `full_burst_start` — **자기 버스트 여부와 무관** | `GAMEPLAY §트리거 발동 의미` · 실측 15회 × 4명 = 60건 | `burst_cast`였다면 버퍼 역할이 격 사이클로 반토막 난다 |
| LD 어설트 3 (기본) | `target_stunned` — **이름 있는 상태가 아니라 기절 자체** | parsed `note`(등록 시 판단) | `self_state` 계열이었다면 남이 건 기절로는 안 열린다 |
| LD 어설트 3 (애장품2) | `target_state:타겟 지정` — **자기 이름 상태**로 조건이 바뀐다 | parsed condition · 실측 7회 → 21회 | 기절 조건이 유지됐다면 창이 3초라 발동이 1/3이다 |
| LD 어설트 2·3 | `bonus_damage`지만 `last_bullet` 트리거라 **보류 대상이 아니다** | `_handle_damage_eff()`는 `burst_cast`만 보류 | 보류였다면 풀버스트 밖 발동분이 사라진다 |
| AK 미사일 (애장품1) | `element_bonus_pct` 130% — **우월 코드 성립 시에만** | `damage.py §⑦` · 실측 수냉 ↔ 작열 ×3.16 | 무조건 적용이었다면 이 캐릭터가 적 코드와 무관해진다 |
| AK 미사일 2 | `burst_damage` + `all_enemies` → `burst_dmg_aoe_pct` 대상 | `IMPL-STATUS §burst_dmg_aoe_pct` | — |
| 타겟 지정 | `atk_pct −5.02`를 **적에게** — 딜 계산에는 무영향, 게이트 전용 | 대상 `all_enemies` | 아군 대상 오독이었다면 스쿼드 공격력이 깎인다 |
| 애장품 판본 | 애장품 판본이 기본 판본을 **대체**한다(합산 아님) | 실측 `LD 어설트 2` 56회(중복 없음) | 합산이었다면 히트가 두 배로 잡힌다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 이탈 보고에 **`equip_skills.max_ammo_pct: 129.64 → 0 (레이어)`** 한 줄
- [ ] `last_bullet` 주기: 풀버스트 안 **≈2.5초** / 밖 **≈5.0초** (실측 5.683·8.200·10.717·13.233 → 18.250)
- [ ] `LD 어설트`·`LD 어설트 2` **56회** · `LD 어설트 3` **21회**
- [ ] `LD 어설트 3`이 **`타겟 지정` 창 안에서만** 난다 (자기 버스트 후 10초)
- [ ] `EX 매거진` 1~4가 **매 풀버스트** 전원에게 (표준 15 × 4 = **60건씩**)
- [ ] 풀버스트 중 재장전 시간 **≈0.017초** (`EX 매거진 2`)
- [ ] 애장품 1 → 2에서 `LD 어설트 3` 조건이 **기절 → 타겟 지정**으로 바뀐다 (7회 → 21회)
- [ ] 애장품 0 → 3에서 개인 딜 **×2.48** (194.0M → 481.3M)
- [ ] 장탄 옵션 129.64%로 되돌리면 개인 딜 **−43.2%** (레이어 회귀 항목)
- [ ] 적 코드를 작열로 바꾸면 개인 딜 **×3.16** (`AK 미사일` 130%)
- [ ] `타겟 지정`의 `atk_pct −5.02`가 **스쿼드 딜을 바꾸지 않는다** (네거티브)
- [ ] 프리바티 버스트 간격 **25.067s**, 180초 8회 · 사이클 간격 12.533s
